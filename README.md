# Termtainer Contermer

This is based on [Terminado](https://github.com/jupyter/terminado)

## Usage
`make build`
    
Generates the docker image
    
`make up`

Runs the image. Navigate a browser to `localhost:5001/new`